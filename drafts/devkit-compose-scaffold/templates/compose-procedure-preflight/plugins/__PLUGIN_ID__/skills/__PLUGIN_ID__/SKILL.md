---
name: __PLUGIN_ID__
description: __SKILL_DESCRIPTION_JSON__
---

# __DISPLAY_NAME__

This generated Skill remains a scaffold. Do not install or invoke the provider
as a completed product while `.openadam-scaffold` exists.

Replace this section with the smallest product-specific guidance that changes a
fresh Agent's result:

- the concrete user requests that should route to the tool;
- the ambiguity that requires a human choice before calling it;
- the stable error or uncertainty branches that change the next action;
- the stopping and presentation behavior that is not already enforced by the
  executable schema or core.

Do not copy the input/output schema, algorithm, or a settled Procedure into this
Skill. Keep the executable contract in the provider and remove scaffold wording
before deleting `.openadam-scaffold`.

Do not invent a Capability. Observation is `org.openadam.file.inspect@0.1.0`.
Do not treat File Vitals as `raster.verify`. Do not generate missing sizes.
